/**
 * Mixin is a class that contains methods for use by other classes without having to be the parent class 
 * of those other classes.” In other words, mixins are normal classes from which we can borrow methods
 * (or variables) from without extending the class.
 */
